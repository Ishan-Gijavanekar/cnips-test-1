apiVersion: cnips.io/v1
kind: Component
metadata:
    name: BETTERSTACK-ALERT
spec:
    type: destination
    language: javascript
    description: This will use to handle to create the incidents in the betterstack while the purge pipeline fails
    config:
        ALERT_URL: '[REDACTED]'
    signatureVersion: v3
