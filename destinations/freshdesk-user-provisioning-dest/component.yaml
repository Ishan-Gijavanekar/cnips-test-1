apiVersion: cnips.io/v1
kind: Component
metadata:
    name: freshdesk-user-provisioning-dest
spec:
    type: destination
    language: javascript
    config:
        base_url: https://qa.cidaas.de
        groupId: freshdesk
    signatureVersion: v1
