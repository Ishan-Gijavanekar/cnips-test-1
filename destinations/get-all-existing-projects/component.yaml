apiVersion: cnips.io/v1
kind: Component
metadata:
    name: get_all_existing_projects
spec:
    type: destination
    language: golang
    config:
        cmiBaseURI: https://cmi-dev.cidaas.com/management-srv/v5/admin/release/basemicroservices
        cmiToken: '[REDACTED]'
        gitlab token: '[REDACTED]'
        gitlabBaseURL: '[REDACTED]'
        limit: "10"
        offset: "0"
    signatureVersion: v2
