apiVersion: cnips.io/v1
kind: Component
metadata:
    name: Gitlab Multi Issue Updation
spec:
    type: destination
    language: javascript
    config:
        gitlabToken: '[REDACTED]'
    signatureVersion: v2
