apiVersion: cnips.io/v1
kind: Component
metadata:
    name: GitlabTicketUpdation
spec:
    type: destination
    language: javascript
    config:
        gitlabBaseUrl: '[REDACTED]'
        gitlabToken: '[REDACTED]'
    signatureVersion: v2
