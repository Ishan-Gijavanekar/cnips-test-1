apiVersion: cnips.io/v1
kind: Component
metadata:
    name: GL-DEST-DELETE-USER
spec:
    type: destination
    language: javascript
    config:
        gitlab_token: '[REDACTED]'
        gitlab_url: '[REDACTED]'
    signatureVersion: v1
