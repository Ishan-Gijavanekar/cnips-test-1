/** * Destination Function Contract: * * @param event - JSON object representing the event data * @param config - Destination configuration (read-only) * @param vars - Global variables object (modify properties directly) * @param LOG - Logger instance for this execution * * IMPORTANT: * - DO NOT use require statement, always use import. * - Modify vars properties directly: vars.myProperty = newValue * - DO NOT reassign vars: vars = newObject (this won't work) * - No return values are required as this script is used to send data to destinations. * - You can also use typescript if you want to enforce types * - Just add types to the function parameters and import Logger from "@cnips/simplelog" * * Example: * ```typescript * import { Logger } from "@cnips/simplelog"; * interface MyEvent { type: string; timestamp: Date; } * interface Config { base_url: string; api_key: string; } * interface Veriables { access_token: string; } * export async function execute(event: MyEvent, config: Config, vars: Variables, LOG: Logger) * ``` */ export async function execute(event, config, vars, LOG) { try { LOG.info(`Executing event: ${event}`); LOG.debug(`Config: ${JSON.stringify(config)}`); LOG.debug(`Vars: ${JSON.stringify(vars)}`); LOG.info(`Event details: %s, Config: %j, Vars: %j`, event, config, vars); LOG.info(`Event sent successfully: ${event}`); } catch (error) { LOG.error(`Error executing event ${event}:`, error); throw error; } }