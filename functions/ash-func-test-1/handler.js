module.exports = function (req, res, config) {
    //add you api logic here ...
    // write a greeting function returning the users name
    //remember to return the response
    return res.json({message: "hello world!"});
}