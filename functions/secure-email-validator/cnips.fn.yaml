apiVersion: cnips.io/v1
kind: Function
metadata:
    name: secure-email-validator
spec:
    runtime: go
    description: Create a secure function that validates an email address from the input payload field 'email' and returns whether it is valid. This function handles user data so it must be secured with an API key
    config:
        apikeyheader: '[REDACTED]'
        apikeyvalue: '[REDACTED]'
    apiAccessRef: d3f50723-bd3a-4c17-adc9-5a067330dd82
    signatureVersion: v2
    templateVersion: V2
