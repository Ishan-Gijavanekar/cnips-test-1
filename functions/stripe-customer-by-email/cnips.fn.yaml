apiVersion: cnips.io/v1
kind: Function
metadata:
    name: stripe-customer-by-email
spec:
    runtime: js
    description: Retrieves a Stripe customer by email via the Stripe REST API and returns the first matching customer ID.
    config:
        stripebaseurl: https://api.stripe.com
    signatureVersion: v3
    templateVersion: V3
