apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: a
spec:
    trigger:
        type: http
    steps:
        - id: js-schema-dest
          uses: transformation/ashsource1@latest
        - id: js-schema-dest
          uses: transformation/js-schema-dest@1.0.0
