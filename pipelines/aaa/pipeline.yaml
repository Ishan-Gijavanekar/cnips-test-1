apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: aaa
spec:
    trigger:
        type: http
    steps:
        - id: test-deniz
          uses: transformation/create-update-terminate@latest
        - id: test-deniz
          uses: transformation/www@latest
          with:
            api_access_ref: ""
            method: GET
            payload: ""
            timeout: 30s
            url: https://google.com
        - id: test-deniz
          uses: transformation/test-deniz@latest
