apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: aishaish
spec:
    trigger:
        type: http
    steps:
        - id: bunney-net-cdn-cache-clear
          uses: transformation/otc-obs-event-create@latest
        - id: bunney-net-cdn-cache-clear
          uses: transformation/go-test-script@latest
        - id: bunney-net-cdn-cache-clear
          uses: transformation/bunney-net-cdn-cache-clear@latest
