apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: calendar-form
spec:
    trigger:
        type: http
    steps:
        - id: calendar-form
          uses: transformation/calendar-form@latest
        - id: calendar-form
          uses: transformation/transform-calendar-data@1.0.0
        - id: calendar-form
          uses: transformation/calendar-form@1.0.0
