apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: demo
spec:
    trigger:
        type: http
    steps:
        - id: just-log
          uses: transformation/test-demo@latest
        - id: just-log
          uses: transformation/return-dummy-data@1.0.0
        - id: just-log
          uses: transformation/just-log@1.0.0
