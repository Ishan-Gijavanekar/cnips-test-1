apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: ext-src-pl
spec:
    trigger:
        type: http
    steps:
        - id: js-var-test
          uses: transformation/ext-src@latest
        - id: js-var-test
          uses: transformation/js-var-test@latest
