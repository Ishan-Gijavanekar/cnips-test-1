apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: extract test
spec:
    trigger:
        type: http
    steps:
        - id: d1
          uses: transformation/test-extract@latest
        - id: d1
          uses: transformation/d1@latest
