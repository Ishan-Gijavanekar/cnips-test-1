apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: FD_PL_AGENT_NO_VALIDATION
spec:
    trigger:
        type: http
    steps:
        - id: fd-dest
          uses: transformation/fd-src@latest
        - id: fd-dest
          uses: transformation/fd-getuserinternal@1.0.0
        - id: fd-dest
          uses: transformation/fd-create-agent-xform@1.0.0
        - id: fd-dest
          uses: transformation/fd-dest@1.0.0
