apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: FD_PL_CONTACT_NO_VALIDATION
spec:
    trigger:
        type: http
    steps:
        - id: fd-dest-contact
          uses: transformation/fd-src@latest
        - id: fd-dest-contact
          uses: transformation/fd-getuserinternal@1.0.0
        - id: fd-dest-contact
          uses: transformation/fd-dest-contact@1.0.0
