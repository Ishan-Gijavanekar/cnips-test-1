apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: freshdesk_create_user_flow_pl
spec:
    trigger:
        type: http
    steps:
        - id: dummy-dest-2
          uses: transformation/freshdesk-with-code-src@latest
        - id: dummy-dest-2
          uses: native/decision@1
        - id: dummy-dest-2
          uses: transformation/fd-getuserinternal@1.0.0
        - id: dummy-dest-2
          uses: transformation/dummy-dest@1.0.0
        - id: dummy-dest-2
          uses: transformation/dummy-dest@1.0.0
