apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: g-t2-rel
spec:
    trigger:
        type: http
    retry:
        maxAttempts: 3
        backoffSec: 5
    steps:
        - id: js-dest-nosetip
          uses: transformation/t-g-err-t2-copy-copy@latest
        - id: js-dest-nosetip
          uses: native/decision@1
        - id: js-dest-nosetip
          uses: transformation/test-go-dest1@latest
        - id: js-dest-nosetip
          uses: transformation/js-dest-nosetip@latest
