apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: new 3
spec:
    trigger:
        type: http
    steps:
        - id: ash-all-js-2
          uses: transformation/ash-all-js@latest
        - id: ash-all-js-2
          uses: transformation/ash-all-js@latest
