apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: obs-log
spec:
    trigger:
        type: http
    steps:
        - id: log-res
          uses: transformation/otc-obs-event-create@latest
        - id: log-res
          uses: transformation/log-res@latest
