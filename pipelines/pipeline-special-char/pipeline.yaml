apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: pipeline-special-char
spec:
    trigger:
        type: http
    steps:
        - id: dest-special-char
          uses: transformation/src-special-char-rename@latest
        - id: dest-special-char
          uses: transformation/dest-special-char@latest
