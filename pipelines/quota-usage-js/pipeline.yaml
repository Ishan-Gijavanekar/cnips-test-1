apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: quota usage js
spec:
    trigger:
        type: http
    retry:
        maxAttempts: 1
        backoffSec: 60
    steps:
        - id: quota-usage-js
          uses: transformation/quota-usage-js@latest
        - id: quota-usage-js
          uses: transformation/quota-usage-js@1.0.0
        - id: quota-usage-js
          uses: transformation/quota-usage-js@1.0.0
