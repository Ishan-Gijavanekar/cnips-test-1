apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: TEST-ED-PL
spec:
    trigger:
        type: http
    steps:
        - id: brevo-sendmail-dest
          uses: transformation/test-ed-src@latest
        - id: brevo-sendmail-dest
          uses: transformation/brevo-sendmail-dest@1.0.0
