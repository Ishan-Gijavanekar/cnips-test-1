apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: test-extractor-ingestion
spec:
    trigger:
        type: http
    steps:
        - id: hello-test
          uses: transformation/test-extractor-ingestion@latest
        - id: hello-test
          uses: transformation/hello-test@latest
