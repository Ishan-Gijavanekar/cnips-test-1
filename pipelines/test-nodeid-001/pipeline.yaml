apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: test-nodeid-001
spec:
    trigger:
        type: http
    steps:
        - id: template-dest
          uses: transformation/t-g-sc-13@latest
        - id: template-dest
          uses: transformation/template-dest@latest
