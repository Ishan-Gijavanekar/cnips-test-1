apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: test-pipeline
spec:
    trigger:
        type: http
    steps:
        - id: test1-source
          uses: transformation/test1-source@latest
