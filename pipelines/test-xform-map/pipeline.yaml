apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: test-xform-map
spec:
    trigger:
        type: http
    steps:
        - id: test
          uses: transformation/mytest@latest
        - id: test
          uses: transformation/test@latest
