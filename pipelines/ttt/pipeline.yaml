apiVersion: cnips.io/v1
kind: Pipeline
metadata:
    name: ttt
spec:
    trigger:
        type: http
    steps:
        - id: js-var-test
          uses: transformation/ttt@latest
        - id: js-var-test
          uses: transformation/js-var-test@latest
