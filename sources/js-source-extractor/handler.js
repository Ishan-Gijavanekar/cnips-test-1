module.exports = function(config) { /* required */
    //add your script here to pull data from your data source.
    let data = [{sub: "sub100"}, {sub: "sub200"}, {sub: "sub300"}];
    //always return your data as an array.
    return data;
}