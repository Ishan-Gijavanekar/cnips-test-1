apiVersion: cnips.io/v1
kind: Component
metadata:
    name: appr-pyth
spec:
    type: approval
    language: python
    config:
        email_from: innacy.raja@widas.in
        sendgrid_api_key: '[REDACTED]'
    signatureVersion: v1
