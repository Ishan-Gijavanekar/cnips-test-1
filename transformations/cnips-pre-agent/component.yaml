apiVersion: cnips.io/v1
kind: Component
metadata:
    name: cnips-pre-agent
spec:
    type: transformation
    language: javascript
    config:
        gitlab_token: '[REDACTED]'
        gitlab_tokenq: '[REDACTED]'
    signatureVersion: v3
