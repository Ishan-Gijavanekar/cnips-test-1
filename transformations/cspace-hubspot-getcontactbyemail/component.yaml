apiVersion: cnips.io/v1
kind: Component
metadata:
    name: '[cspace]Hubspot_GetContactByEmail'
spec:
    type: transformation
    language: golang
    description: Based on the email of a cidaasUser this transformation will search for an contact with same email in hubspot and add it to the event.
    config:
        hubspotBaseURL: '[REDACTED]'
        hubspotToken: '[REDACTED]'
    signatureVersion: v2
