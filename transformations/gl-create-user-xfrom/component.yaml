apiVersion: cnips.io/v1
kind: Component
metadata:
    name: GL_CREATE_USER_XFROM
spec:
    type: transformation
    language: javascript
    config:
        gitlab_token: '[REDACTED]'
        gitlab_token1: '[REDACTED]'
        gitlab_url: '[REDACTED]'
        groupId: gitlab
    signatureVersion: v1
