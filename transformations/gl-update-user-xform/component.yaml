apiVersion: cnips.io/v1
kind: Component
metadata:
    name: GL_UPDATE_USER_XFORM
spec:
    type: transformation
    language: javascript
    config:
        gitlab_token: '[REDACTED]'
        gitlab_url: '[REDACTED]'
    signatureVersion: v1
