apiVersion: cnips.io/v1
kind: Component
metadata:
    name: test-appr
spec:
    type: approval
    language: javascript
    config:
        email_from: innacy.raja@widas.in
        sendgrid_api_key: '[REDACTED]'
        test: widas
    signatureVersion: v3
