apiVersion: cnips.io/v1
kind: Component
metadata:
    name: transform-calendar-data
spec:
    type: transformation
    language: javascript
    config:
        apikey: '[REDACTED]'
    signatureVersion: v2
